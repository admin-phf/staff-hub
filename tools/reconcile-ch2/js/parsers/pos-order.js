/*
  POS BACK-END ORDER PARSER
  -------------------------
  This file will contain only the rules for reading the POS order export.
  Keeping it separate means changes to POS order format do not touch the CH2 parser or UI.
*/
export function parsePosOrder(/* arrayBuffer, filename */){
  throw new Error('POS order parser not connected yet.');
}
